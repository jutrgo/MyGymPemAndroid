<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="proyecto.pem.mygym">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="proyecto.pem.mygym.AppMediador">


        <activity
            android:name="proyecto.pem.mygym.ejercicios.view.EjerciciosView"
            android:label="@string/app_name">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN" />-->

            <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>



        <activity
        android:name="proyecto.pem.mygym.home.HomeView"
        android:label="@string/app_name">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity
            android:name="proyecto.pem.mygym.menu.MenuView"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="proyecto.pem.mygym.category.view.CategoryView"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="proyecto.pem.mygym.estiramientos.EstiramientosView"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="proyecto.pem.mygym.rutinas.view.RutinasView"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="proyecto.pem.mygym.nuevaRutina.view.NuevaRutinaView"
            android:label="@string/app_name">

        </activity>


    </application>

</manifest>